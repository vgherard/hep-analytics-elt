# TODO: fill absolute paths via script

hep_analytics:
  outputs:
    prod:
      type: sqlite

      # sqlite locks the whole db on writes so anything > 1 won't help
      threads: 1

      # This value is arbitrary
      database: "database"


      schema: 'main'

      schemas_and_paths:
        main: '/Users/valerio/Code/personal/hep-analytics-elt/db/db.sqlite'
        raw: '/Users/valerio/Code/personal/hep-analytics-elt/db/raw.sqlite'

      schema_directory: '/Users/valerio/Code/personal/hep-analytics-elt/db'

      #extensions:
        #- '/path/to/sqlite-digest/digest.so'

  target: prod